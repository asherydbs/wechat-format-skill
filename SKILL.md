---
name: wechat-format
description: >
  将 Markdown 文章转换成微信公众号可直接粘贴发布的 HTML 排版文件。
  根据文章内容自动匹配最佳风格——随笔用温墨暖棕、教程用青蓝清爽、观点用赤丹朱红。
  使用场景：公众号文章定稿后，执行一键转换。
  触发词："转公众号"、"微信排版"、"wechat"、"公众号格式"、"排版一下"。
---

# wechat-format — 排版组件化公众号转换

## 设计哲学

这个 skill 不是"套模板"。核心工作流是：

1. **读文章** — 理解内容结构，判断哪里该用什么排版组件
2. **手动插入组件标记** — 在 markdown 中加卡片、标题装饰、分割线等
3. **选风格** — 根据内容匹配最合适的排版调性
4. **执行转换** — 用脚本产出微信兼容的 HTML

**关键：** 排版组件由你（AI）手动添加。读完文章后，判断哪里适合放卡片、哪里要装饰标题、哪个金句值得突出。这是设计劳动，不是自动化。

## 三步工作流

### 第一步：读文章 + 判断风格

1. 读取 Markdown 文件内容
2. 快速判断文章性质：
   - 个人经历、叙事、散文 → 温墨 (ink)
   - 技术教程、操作指南 → 青蓝 (azure)
   - 观点、评论、分析 → 赤丹 (cinnabar)

### 第二步：加组件标记 + 执行转换

在 markdown 中添加合适的组件标记（见下方组件库），然后运行：

```bash
python3 ~/.hermes/skills/wechat-format/scripts/wechat-format.py input.md output.html --style=ink
```

- `--style` 可选（ink/azure/cinnabar），不指定则脚本自动分析——但你的判断更准，主动指定优先
- `output` 可选，缺省在 input 同目录生成 `input.wechat.html`
- 注意：`output` 位置参数要放在 `--style` 前面，否则 argparse 的 nargs="?" 会吞掉 style 的值

### 第三步：验证与交付

输出后至少验证以下几点：
- ✅ 所有组件是否渲染正确（卡片内容不丢失、金句有左框、banner 粗体是白色）
- ✅ 标题是否居中且有层次
- ✅ 引用块有左边框
- ✅ 代码块有语法高亮
- ✅ 全文无 `<style>` 或 class 选择器（全部内联）
- ✅ 全文无 `<script>`、`<iframe>` 等微信禁用标签

**报告结果时只说事实：** "已生成: /path/to/file.html" + 风格 + 文件大小。不要加评语。

## 三种风格

### 📝 温墨 (ink)
- **适合：** 随笔、散文、叙事、小说
- **色调：** 暖棕 `#8B6914`，牙白底 `#FAF8F5`
- **正文：** 17px，行高 1.85，`#3D3D3D`

### 🔷 青蓝 (azure)
- **适合：** 技术教程、方法论、指南
- **色调：** 蓝色 `#2B6CB0`，浅蓝底 `#F7F8FA`
- **正文：** 16px，行高 1.75

### 🔥 赤丹 (cinnabar)
- **适合：** 观点文、评论、深度分析
- **色调：** 朱红 `#C0392B`，暖灰底 `#F9F5F4`
- **正文：** 17px，行高 1.8

所有风格均使用系统原生字体，无版权风险。

## 排版组件库

### 一、卡片区块（7 种）

卡片用 `:::` 围栏标记，支持多段落和内联格式。

#### 白色卡片 — card.white
```markdown
::: card.white :::
这是白色圆角卡片，内容独立于正文。
支持多段落。
:::
```
渲染为白底 + 浅色边框 + 圆角。适合包裹一组相关短段落。

#### 高亮卡片 — card.highlight
```markdown
::: card.highlight :::
这是需要读者特别注意的内容，背景为风格主色调的淡色。
:::
```
淡色背景 + 圆角。适合情感段落、重要说明。

#### 金句引用 — card.quote
```markdown
::: card.quote :::
看不起作弊的核心原因不是道德，是**骗自己**。
:::
```
粗左边框 + 大字号（19px）+ 主色调加粗。文章中最有力量的句子。

#### 提示卡片 — card.tip
```markdown
::: card.tip :::
用短期捷径换来的"效率"，最终都会以更大的成本还回去。
:::
```
左竖线 + 💡 提示标记。适合补充说明、注意事项。

#### 横幅 — card.banner
```markdown
::: card.banner :::
**作弊是用今天的捷径，**
**买明天的债。**
:::
```
全主色调背景 + 白色大字居中。适合号召性语句、核心金句。注意：使用 `**` 粗体时文字保持白色。

#### 丝带 — card.ribbon
```markdown
::: card.ribbon :::
短期来看，作弊是捷径。长期来看，捷径就是最远的路。
:::
```
淡底色色带，居中加粗。适合过渡性金句、概括性陈述。

#### 旧兼容写法（无需 `.` 后缀）
- `::: card :::` → 等价于 card.white
- `::: highlight :::` → 等价于 card.highlight
- `::: quote :::` → 等价于 card.quote
- `::: tip :::` → 等价于 card.tip

### 二、标题装饰（5 种）

在 markdown 标题后加 `[装饰名]`：

#### 装饰线 — [装饰线]
```markdown
## [装饰线] 章节标题
```
左右水平线 + 居中标题文字。适合大章节分割。

#### 左框 — [左框]
```markdown
## [左框] 小节标题
```
厚左边框 + 主色调。适合小节标题。

#### 圆标 — [圆标]
```markdown
## [圆标] 一、要点名称
## [圆标] 二、另一个要点
```
圆形数字/序号徽章。适合并列要点、步骤序号。

#### 下划线 — [下划线]
```markdown
## [下划线] 重点标题
```
居中 + 下划线装饰。适合强调某个标题。

#### 色带 — [色带]
```markdown
## [色带] 分类标题
```
淡底色块 + 左竖线。适合分类标签式标题。

### 三、分割线变体（5 种）

| 写法 | 效果 |
|------|------|
| `---` | 实线（标准分割） |
| `===` | 虚线 |
| `***` | 星号装饰（✦ ✦ ✦） |
| `~~~` | 波浪装饰（~ ~ ~） |
| `...` | 圆点短条 |

### 四、内联装饰（4 种）

| 写法 | 效果 |
|------|------|
| `==高亮文字==` | 黄色背景高亮（不改变上下文粗细） |
| `!!强调文字!!` | 主色调加粗 |
| `((标签文字))` | 浅边框小标签 |
| `` `短标签` `` | 内联圆角小标签（≤10字符时自动变为药丸样式） |

注意：`!!强调!!` 不要与自然出现的多感叹号混淆。使用时确保 `!!` 正确成对。

## 使用建议

一篇 2000 字左右的文章，组件数的合理范围：

| 组件类型 | 建议用量 | 说明 |
|----------|----------|------|
| 卡片 | 2-4 个 | 太多会打断阅读节奏 |
| 标题装饰 | 3-5 个 | 每个主要章节一个 |
| 分割线变体 | 1-2 个 | 大章节切换时用 `[装饰线]` 或 `***` |
| 内联装饰 | 3-6 处 | ==高亮== 和 !!强调!! 交错使用 |

**使用模式推荐：**

观点文：`card.quote`（金句）+ `[左框]`（小节）+ `==高亮==`（关键概念）
教程文：`card.tip`（注意事项）+ `[圆标]`（步骤）+ `` `标签` ``（术语）
叙事文：`card.highlight`（感触片段）+ `[装饰线]`（时间线）+ `***`（场景切换）

## 支持的 Markdown 元素

| Markdown | 转换结果 |
|----------|----------|
| `# ~ ######` | 各级标题（h1 居中，h2 左边框） |
| `**bold**` | 加粗（颜色 = 风格主色调） |
| `*italic*` | 斜体 |
| `~~strike~~` | 删除线 + 灰色 |
| `> quote` | 引用块（左边框 + 浅背景） |
| `` `code` `` | 内联代码（短 → 药丸标签，长 → 常规） |
| ` ``` ` + lang | 代码块（Pygments 语法高亮 + 灰底圆角） |
| `[text](url)` | 链接（主色调 + 下边框） |
| `![alt](url)` | 图片（居中 + 圆角） |
| `- list` / `1. list` | 列表 |
| GFM table | 表格（主色调表头） |
| `---` / `===` / `***` / `~~~` / `...` | 分割线（5 种变体） |
| YAML frontmatter `title:` | 文章标题（自动提取） |

## 陷阱（从教训中来）

- **不要在 banner 卡片内用 `**粗体**` 以外的强调标记。** `==高亮==` 在 banner 里会继承容器颜色（白色），看不清。banner 内容尽量简洁，用字号和居中自带的强调感。
- **`!!强调!!` 不要与自然感叹号相邻。** `!!!text!!!` 会丢失首尾的 `!`。如果需要三个感叹号，用空格隔开或用 `==text==` 代替。
- **`:::` 围栏的闭合必须严格三冒号。** `:::` 开头 → `:::` 结尾（独占一行）。`::` 不会被识别。
- **卡片内容的多段落用双换行分隔。** 单换行会被视为同一段落中的 `<br>`。
- **Pygments 的 class 属性：** 代码语法高亮必须用 `noclasses=True`（内联样式），否则输出 `<span class="k">` 会被微信过滤。
- **argparse nargs="?" + 可选标志的相互干扰。** 当输出路径跟在 `--style` 后面时，argparse 会把 `--style` 后面的值当成 output。调用时把 output 放 `--style` 之前。

## 注意事项

- YAML frontmatter 的 `title:` 字段会被提取为文章标题
- 输出文件用浏览器打开后 Ctrl+A 全选复制，粘贴到微信编辑器即可
- 超出 10MB 的图片微信无法上传，需要先压缩
- GitHub: https://github.com/asherydbs/wechat-format-skill

## References

- `references/wechat-constraints.md` — 微信公众号渲染约束速查
- `scripts/wechat-format.py` — 转换脚本（支持所有组件）
