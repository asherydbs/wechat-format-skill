---
name: wechat-format
description: >
  将 Markdown 文章转换成微信公众号可直接粘贴发布的 HTML 排版文件。
  根据文章内容自动匹配最佳风格——随笔用温墨暖棕、教程用青蓝清爽、观点用赤丹朱红。
  使用场景：公众号文章定稿后，执行一键转换。
  触发词："转公众号"、"微信排版"、"wechat"、"公众号格式"、"排版一下"。
---

# wechat-format — 文章内容感知的公众号排版

## 设计哲学

这个 skill 不是"套模板"。核心工作流是：

1. **读文章** — 理解内容类型（随笔/教程/观点）
2. **选风格** — 根据内容匹配最合适的排版调性
3. **执行转换** — 用脚本产出微信兼容的 HTML
4. **验证** — 确认关键元素排版正确

## 三步工作流

### 第一步：读文章

当你收到"帮我把这篇文章转成公众号格式"时：

1. 读取 Markdown 文件内容
2. 快速判断文章性质：
   - 是个人经历、叙事、散文？ → 温墨 (ink)
   - 是技术教程、操作指南？ → 青蓝 (azure)
   - 是观点、评论、分析？ → 赤丹 (cinnabar)
3. 告诉用户你的判断和理由，确认后再执行

### 第二步：执行转换

```bash
python3 ~/.hermes/skills/wechat-format/scripts/wechat-format.py input.md [--style ink|azure|cinnabar] [output.html]
```

- `--style` 可选，不指定则脚本自动分析（但你的判断比你准，最好你主动指定）
- 不指定 output 路径时，在 input 同目录生成 `input.wechat.html`

### 第三步：验证

输出后至少验证以下几点：
- ✅ 标题是否居中且有层次
- ✅ 引用块有左边框
- ✅ 代码块有语法高亮且样式不与内嵌标签冲突
- ✅ 全文无 `<style>` 或 class 选择器（全部内联）
- ✅ 全文无 `<script>`、`<iframe>` 等微信禁用标签

## 三种风格

### 📝 温墨 (ink)
- **适合：** 随笔、散文、叙事、小说
- **色调：** 暖棕 `#8B6914`，牙白底
- **气质：** 雅致、留白通透，字距略宽
- **正文：** 17px，行高 1.85，`#3D3D3D`
- **h2：** 暖棕左边框 3px，无衬线，非粗体

### 🔷 青蓝 (azure)
- **适合：** 技术教程、方法论、指南
- **色调：** 蓝色 `#2B6CB0`，浅蓝底
- **气质：** 清爽、结构清晰、功能性强
- **正文：** 16px，行高 1.75
- **代码：** 灰色块 + 前蓝后蓝

### 🔥 赤丹 (cinnabar)
- **适合：** 观点文、评论、深度分析
- **色调：** 朱红 `#C0392B`，暖灰底
- **气质：** 对比强、有力量感、自信
- **正文：** 17px，行高 1.8
- **引用：** 18px 加粗朱红左边框，有宣言感

所有风格均使用系统原生字体，无版权风险：
```
-apple-system, BlinkMacSystemFont, 'PingFang SC',
'Microsoft YaHei', 'Helvetica Neue', Arial, sans-serif
```

## 支持的内容元素

| Markdown | 转换结果 |
|----------|----------|
| `# ~ ######` | 各级标题（h1 居中，h2 左边框） |
| `**bold**` | 加粗（颜色 = 风格主色调） |
| `*italic*` | 斜体 |
| `~~strike~~` | 删除线 + 灰色 |
| `<u>text</u>` | 下划线 |
| `> quote` | 引用块（左边框 + 浅背景） |
| `` `code` `` | 内联代码（主色调） |
| ```` ```lang ```` | 代码块（语法高亮 + 灰底圆角） |
| `[text](url)` | 链接（主色调 + 下边框） |
| `![alt](url)` | 图片（居中 + 圆角） |
| `- list` / `1. list` | 列表 |
| GFM table | 表格（主色调表头） |
| `---` | 分割线 |
| YAML frontmatter `title:` | 文章标题（自动提取） |

## 陷阱（从教训中来）

- **不要套固定模板。** 第一次设计这个 skill 时我写了一组固定 CSS，被用户指出"是根据内容设计排版，不是套模板"。修改后才有三套风格+内容分析。每次执行时先读文章、判断类型、选风格——风格选择本身就是输出质量的一部分。
- **脚本的自动检测不可全信。** `--style` 不指定时用关键词分析，但混合内容（如随笔中有一个代码块）可能误判。用你自己的判断覆盖脚本分析，确认后再执行。
- **Pygments 的 class 属性。** 代码语法高亮必须用 `noclasses=True`（内联样式），否则输出 `<span class="k">` 会被微信过滤。
- **`<s>` 和 `<u>` 的正则可能误吞其他标签。** `<s` 会匹配 `<span>`，`<u` 会匹配 `<ul>`。脚本中已用 `(?![a-zA-Z])` 做词边界检查。
- **图片占位。** 输出中的 `<img src="...">` 在微信中会被转存到 `mmbiz.qpic.cn`。原文 src 只是占位。

## 注意事项

- YAML frontmatter 的 `title:` 字段会被提取为文章标题
- 输出文件用浏览器打开后 Ctrl+A 全选复制，粘贴到微信编辑器即可
- 超出 10MB 的图片微信无法上传，需要先压缩

## References

- `references/wechat-constraints.md` — 微信公众号渲染约束速查（支持的标签、禁用的CSS、媒体限制）
- `references/style-mapping.md` — 文章内容→排版风格映射指南（含边界案例和判据）
- `scripts/wechat-format.py` — 转换脚本（支持 --style 参数和自动内容分析）
